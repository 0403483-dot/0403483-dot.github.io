// Array/Object Noatation Assignment
// Jonathan Hlady
// March 5/26
//
// Extra for Experts:
// - describe what you did to take this project "above and beyond"


let tower = [
  [floor(random(0,2)),floor(random(0,2)),floor(random(0,2))],
  [floor(random(0,2)),floor(random(0,2)),floor(random(0,2))],
  [floor(random(0,2)),floor(random(0,2)),floor(random(0,2))],
  [floor(random(0,2)),floor(random(0,2)),floor(random(0,2))],
  [floor(random(0,2)),floor(random(0,2)),floor(random(0,2))],
  
];

let bet = 25;
let betMax = 500;
let betMin = 25;
let currentRow = 0;


function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(220);
}


function drawTower(){
  for (let tile of tower){
    
  }
}